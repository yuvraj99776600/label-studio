url: https://docs.mltl.us/

theme_config:
  tier: enterprise
  root_domain: "mltl.com"

# Deployment
deploy:
  type: aws-s3
  region: us-east-1
  bucket: docs.heartex.com

sitemap:
  path:
    - guide/sitemap-hsdocs.xml
